{% extends 'Admin/base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('style/css/custom/table.css') }}">
    <link rel="stylesheet" href="{{ asset('style/css/custom/spinner.css') }}">
    <link rel="stylesheet" href="{{ asset('style/css/select2/select2.min.css') }}">
    <link rel="stylesheet" href="{{ asset('style/css/select2/select2-bootstrap4.min.css') }}">

    {{ parent() }}
{% endblock %}

{% block title %}Events | {{ event.title }}{% endblock %}

{% block body %}
    <h2 class="border-bottom">{{ event.title }}</h2>
    <div class="row">
        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">
                        Event details
                        <span class="pull-right">
                            <a href="{{ path('event_update', { 'eventId': event.id }) }}">
                                <i class="fa fa-edit"></i>
                            </a>
                        </span>
                    </h5>
                </div>
                <div class="card-body p-0">
                    <div class="container">
                        <div class="row border-bottom">
                            <div class="col-md-4">
                                Date
                            </div>
                            <div class="col-md-8">
                                {{ event.date|date('Y-m-d H:i:s') }}
                            </div>
                        </div>
                        {% if event.endDate is not null %}
                            <div class="row border-bottom">
                                <div class="col-md-4">
                                    End Date
                                </div>
                                <div class="col-md-8">
                                    {{ event.endDate|date('Y-m-d H:i:s') }}
                                </div>
                            </div>
                        {% endif %}
                        <div class="row border-bottom">
                            <div class="col-md-4">
                                Place
                            </div>
                            <div class="col-md-8">
                                {{ event.place }}
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4">
                                Description
                            </div>
                            <div class="col-md-8">
                                {{ event.description }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-8">
            <ul class="nav nav-tabs" id="myTab" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" data-toggle="tab" href="#categories_tab" role="tab"
                       aria-controls="categories_tab"
                       aria-selected="true">Categories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-toggle="tab" href="#workshops_tab" role="tab" aria-controls="workshops_tab"
                       aria-selected="false">Workshops</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-toggle="tab" href="#registrations_tab" role="tab"
                       aria-controls="registrations_tab"
                       aria-selected="false">Registrations</a>
                </li>
            </ul>
            <div class="tab-content">
                <div class="tab-pane fade show active" id="categories_tab" role="tabpanel"
                     aria-labelledby="categories_tab">
                    {% include 'Admin/Event/tabs/categories.html.twig' %}
                </div>
                <div class="tab-pane fade" id="workshops_tab" role="tabpanel" aria-labelledby="workshops_tab">
                    {% include 'Admin/Event/tabs/workshops.html.twig' %}
                </div>
                <div class="tab-pane fade" id="registrations_tab" role="tabpanel" aria-labelledby="registrations_tab">
                    {% include 'Admin/Event/tabs/registrations.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascript_inline %}
    {{ parent() }}
    <script type="text/javascript">
        $(document).ready(function () {
            $('[data-toggle="tooltip"]').tooltip();

            {% if event.categories is not empty %}
            var categoriesList = Sortable.create(document.getElementById('categoriesList'), {
                handle: '.category-list-handle',
                animation: 150,
                onUpdate: function () {
                    categoriesList.option("disabled", true);
                    $('#categories_spinner').addClass('spinner');
                    $('.category-list-handle').addClass('text-muted');
                    $.post(
                        '{{ path('event_save_category_order', {'eventId': event.id}) }}',
                        {
                            'order': categoriesList.toArray()
                        }
                    ).done(function () {
                        categoriesList.option("disabled", false);
                        $("#categories_spinner").removeClass('spinner');
                        $('.category-list-handle').removeClass('text-muted');
                    });
                }
            });
            {% endif %}

            var workshopModalSelect = $("#workshopModalSelect").select2({
                'placeholder': 'Select Registration Form',
                theme: "bootstrap4",
                dropdownParent: $('#workshopModal')
            });

            $('#workshopModal').on('shown.bs.modal', function () {
                workshopModalSelect.select2('open');
            });

            $("#workshopModalForm").submit(function () {
                $("#workshopModal").modal('hide');
            });

            var categoryModalSelect = $("#categoryModalSelect").select2({
                'placeholder': 'Select Registration Form',
                theme: "bootstrap4",
                dropdownParent: $('#categoryModal')
            });

            $('#categoryModal').on('shown.bs.modal', function () {
                categoryModalSelect.select2('open');
            });

            $("#categoryModalForm").submit(function () {
                $("#categoryModal").modal('hide');
            });
        });
    </script>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('js/sortablejs/Sortable.min.js') }}"></script>
    <script src="{{ asset('js/select2/select2.full.min.js') }}"></script>
{% endblock %}
