{% if config is not empty %}
    {% for item in config %}
        <a class="nav-item nav-link {% if item.active is defined and item.active %}active{% endif %}"
           href="{{ path(item.route) }}">{{ item.title|trans }}</a>
    {% endfor %}
{% endif %}
